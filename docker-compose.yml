services:
  qdrant:
    image: qdrant/qdrant:latest
    container_name: knowledgevault-qdrant
    ports:
      - "6333:6333" # REST API (used by the backend)
      - "6334:6334" # gRPC
    volumes:
      - qdrant_storage:/qdrant/storage
    restart: unless-stopped

volumes:
  qdrant_storage:
